import { AtendimentoAtendidoDirective } from './atendimento-atendido.directive';

describe('AtendimentoAtendidoDirective', () => {
  it('should create an instance', () => {
    const directive = new AtendimentoAtendidoDirective();
    expect(directive).toBeTruthy();
  });
});
